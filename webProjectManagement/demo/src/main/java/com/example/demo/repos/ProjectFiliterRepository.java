package com.example.demo.repos;

import com.example.demo.entities.ProjectFiliter;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectFiliterRepository extends JpaRepository<ProjectFiliter,Long> {
}
